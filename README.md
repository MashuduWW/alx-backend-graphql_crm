# Django Project with GraphQL

A basic Django project integrated with Graphene-Django to provide a GraphQL API.  
Includes a simple `hello` query that returns `"Hello, GraphQL!"`.

##  Requirements

- Python 3.10+
- pip (Python package manager)
- Virtual environment 

